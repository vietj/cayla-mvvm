import io.cayla.mvvm { ModelContext }
import io.cayla.mvvm.view { ViewContext }

"""The binding context binds the model context and the view context"""
shared class BindingContext(shared ModelContext model, shared ViewContext view) {
    
}